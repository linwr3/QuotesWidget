<Window x:Class="QuotesWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuotesWidget"
        mc:Ignorable="d"
        Title="QuotesWidget" 
        Height="200" Width="400"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="WidthAndHeight" 
        Topmost="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="ToolbarBtn" TargetType="Button">
            <Setter Property="Width" Value="25"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Background" Value="#DDFFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ToolbarToggleBtn" TargetType="ToggleButton">
            <Setter Property="Width" Value="25"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Background" Value="#DDFFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF88CC88"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" Background="#E6333333" CornerRadius="10" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">

                <StackPanel x:Name="LockPinPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="BtnLock" Content="🔓" Click="BtnLock_Click" ToolTip="锁定/解锁 (锁住后隐藏背景)" Style="{StaticResource ToolbarBtn}"/>
                    <Button x:Name="BtnPin" Content="📌" Click="BtnPin_Click" ToolTip="置顶开关" Margin="5,0,0,0" Style="{StaticResource ToolbarBtn}"/>
                </StackPanel>

                <StackPanel x:Name="TopControlsPanel" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnSettings" Content="⚙" Click="BtnSettings_Click" ToolTip="设置" Style="{StaticResource ToolbarBtn}"/>
                    <Button x:Name="BtnExit" Content="❌" Click="BtnExit_Click" ToolTip="退出" Margin="5,0,0,0" Style="{StaticResource ToolbarBtn}"/>
                </StackPanel>
            </Grid>

            <local:OutlinedTextBlock x:Name="QuoteText" 
                                     Grid.Row="1" 
                                     Text="LOADING..." 
                                     FontSize="24"
                                     Fill="White"
                                     Stroke="Black"
                                     StrokeThickness="1.5"
                                     Margin="10"
                                     HorizontalAlignment="Center"
                                     />

            <StackPanel Grid.Row="2" Margin="0,15,0,0">

                <StackPanel x:Name="ManualSwitchPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Button x:Name="BtnPrev" Content="◀" Click="BtnPrev_Click" Style="{StaticResource ToolbarBtn}" Width="35"/>
                    <TextBlock Text=" 切换 " VerticalAlignment="Center" Margin="5,0" Foreground="LightGray"/>
                    <Button x:Name="BtnNext" Content="▶" Click="BtnNext_Click" Style="{StaticResource ToolbarBtn}" Width="35"/>
                </StackPanel>

                <StackPanel x:Name="StyleControlsPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="B" Click="BtnBold_Click" FontWeight="Bold" Style="{StaticResource ToolbarBtn}"/>
                    <Button Content="I" Click="BtnItalic_Click" FontStyle="Italic" Style="{StaticResource ToolbarBtn}"/>
                    <Button Content="+" Click="BtnPlus_Click" Style="{StaticResource ToolbarBtn}"/>
                    <Button Content="-" Click="BtnMinus_Click" Style="{StaticResource ToolbarBtn}"/>
                    <ToggleButton x:Name="BtnOutlineToggle" Content="O" Click="BtnOutlineToggle_Click" IsChecked="True" Style="{StaticResource ToolbarToggleBtn}"/>

                    <Border Width="1" Background="Gray" Margin="5,2"/>

                    <Button x:Name="BtnTextColor" Content="A" Click="BtnTextColor_Click" ToolTip="文字颜色" FontWeight="Bold" Style="{StaticResource ToolbarBtn}"/>
                    <Button x:Name="BtnStrokeColor" Content="✒️" Click="BtnStrokeColor_Click" ToolTip="描边颜色" Visibility="Visible" Style="{StaticResource ToolbarBtn}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>

</Window>
